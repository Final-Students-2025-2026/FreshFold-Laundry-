node_modules/
dist/
build/
web-build/
coverage/
*.orig.*
.DS_Store
*.log

# Env files — every app keeps a committed .env.example instead
.env*
!.env.example

# Expo
.expo/
apps/mobile/expo-env.d.ts

# Native project folders (generated by `npx expo prebuild`)
apps/mobile/ios/
apps/mobile/android/

# Server's local JSON datastore
apps/server/data/

# Scratch git worktrees. Each is a full checkout with its own node_modules,
# living inside the repo it belongs to — tracking them would commit the
# repository into itself.
.claude/worktrees/
.claude
